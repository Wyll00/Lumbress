# En dev, déjalo vacío: Vite proxea /api -> http://localhost:5001 (ver vite.config.js).
# En prod, define la URL absoluta del backend si vive en otro dominio.
# Si lo defines, el backend debe enviar Access-Control-Allow-Credentials y la cookie
# necesita SameSite=None + HTTPS (NODE_ENV=production en backend).
VITE_API_URL=
